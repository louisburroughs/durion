# Order Management Backend API Reference (Generated)

**Document Type:** Generated API Reference  
**Audience:** Backend developers, Frontend developers, API consumers  
**Last Updated:** 2026-07-16  
**OpenAPI Source:** `pos-order/openapi.yaml`
**Do Not Edit:** This file is generated by script; update OpenAPI source instead.

---

## Overview

This document is the generated API reference for the Order Management domain.
It is derived from the module OpenAPI specification and should be used for endpoint
and schema details.

For behavior rules, capability intent, and UI task mapping, use the curated domain
`BACKEND_CONTRACT_GUIDE.md`.

---

## Table of Contents

1. [JSON Field Naming Conventions](#json-field-naming-conventions)
2. [Data Types & Formats](#data-types--formats)
3. [Enum Value Conventions](#enum-value-conventions)
4. [Identifier Naming](#identifier-naming)
5. [Timestamp Conventions](#timestamp-conventions)
6. [Collection & Pagination](#collection--pagination)
7. [Error Response Format](#error-response-format)
8. [Correlation ID & Request Tracking](#correlation-id--request-tracking)
9. [API Endpoints](#api-endpoints)
10. [Entity-Specific Contracts](#entity-specific-contracts)
11. [Examples](#examples)

---

## JSON Field Naming Conventions

### Standard Pattern: camelCase

All JSON field names **MUST** use `camelCase` (not `snake_case`, not `PascalCase`).

```json
{
  "id": "abc-123",
  "createdAt": "2026-01-27T14:30:00Z",
  "updatedAt": "2026-01-27T15:45:30Z",
  "status": "ACTIVE"
}
```

### Rationale

- Aligns with JSON/JavaScript convention
- Matches Java property naming after Jackson deserialization
- Consistent with REST API best practices (RFC 7231)
- Consistent across all Durion platform domains

---

## Data Types & Formats

### String Fields

Use `string` type for:

- Names and descriptions
- Codes and identifiers
- Free-form text
- Enum values (serialized as strings)

```java
private String id;
private String name;
private String description;
private String status;
```

### Numeric Fields

Use `Integer` or `Long` for:

- Counts (page numbers, total results)
- Version numbers
- Sequence numbers

```java
private Integer pageNumber;
private Integer pageSize;
private Long totalCount;
```

### Boolean Fields

Use `boolean` for true/false flags:

```java
private boolean isActive;
private boolean isPrimary;
private boolean hasPermission;
```

### UUID/ID Fields

Use `String` for all primary and foreign key IDs:

```java
private String id;
private String parentId;
private String referenceId;
```

### Instant/Timestamp Fields

Use `Instant` in Java; serialize to ISO 8601 UTC in JSON:

```java
@JsonFormat(shape = JsonFormat.Shape.STRING, pattern = "yyyy-MM-dd'T'HH:mm:ss'Z'", timezone = "UTC")
private Instant createdAt;
private Instant updatedAt;
```

JSON representation:

```json
{
  "createdAt": "2026-01-27T14:30:00Z",
  "updatedAt": "2026-01-27T15:45:30Z"
}
```

### LocalDate Fields

Use `LocalDate` for date-only fields (no time component):

```java
@JsonFormat(shape = JsonFormat.Shape.STRING, pattern = "yyyy-MM-dd")
private LocalDate effectiveFrom;
private LocalDate effectiveTo;
```

JSON representation:

```json
{
  "effectiveFrom": "2026-01-01",
  "effectiveTo": "2026-12-31"
}
```

---

## Enum Value Conventions

### Standard Pattern: UPPER_SNAKE_CASE

All enum values **MUST** use `UPPER_SNAKE_CASE`:

```java
public enum Status {
    ACTIVE,
    INACTIVE,
    PENDING_APPROVAL,
    ARCHIVED
}
```

### Enums in this Domain

No enums defined in OpenAPI spec. Standard enum conventions apply:

- Use UPPER_SNAKE_CASE
- Document all possible values
- Avoid numeric codes

---

## Identifier Naming

### Standard Pattern

- Primary keys: `id` or `{entity}Id` (e.g., `customerId`, `orderId`)
- Foreign keys: `{entity}Id` (e.g., `parentId`, `accountId`)
- Composite identifiers: use structured object, not concatenated string

### Examples

```json
{
  "id": "abc-123",
  "customerId": "cust-456",
  "orderId": "ord-789"
}
```

---

## Timestamp Conventions

### Standard Pattern: ISO 8601 UTC

All timestamps **MUST** be:

- Serialized in ISO 8601 format with UTC timezone (`Z` suffix)
- Stored as `Instant` in Java
- Include millisecond precision when available

```json
{
  "createdAt": "2026-01-27T14:30:00.123Z",
  "updatedAt": "2026-01-27T15:45:30.456Z"
}
```

### Common Timestamp Fields

- `createdAt`: When the entity was created
- `updatedAt`: When the entity was last updated
- `deletedAt`: When the entity was soft-deleted (if applicable)
- `effectiveFrom`: Start date for effective dating
- `effectiveTo`: End date for effective dating

---

## Collection & Pagination

### Standard Pagination Request

```json
{
  "pageNumber": 0,
  "pageSize": 20,
  "sortField": "createdAt",
  "sortOrder": "DESC"
}
```

### Standard Pagination Response

```json
{
  "results": [...],
  "totalCount": 150,
  "pageNumber": 0,
  "pageSize": 20,
  "totalPages": 8
}
```

### Guidelines

- Use zero-based page numbering
- Default page size: 20 items
- Maximum page size: 100 items
- Include total count for client-side pagination controls

---

## Error Response Format

### Standard Error Response

All error responses **MUST** follow this format:

```json
{
  "code": "VALIDATION_ERROR",
  "message": "Invalid request parameters",
  "correlationId": "abc-123-def-456",
  "timestamp": "2026-01-27T14:30:00Z",
  "fieldErrors": [
    {
      "field": "email",
      "message": "Invalid email format",
      "rejectedValue": "invalid-email"
    }
  ]
}
```

### Standard HTTP Status Codes

- `200 OK`: Successful GET, PUT, PATCH
- `201 Created`: Successful POST
- `204 No Content`: Successful DELETE
- `400 Bad Request`: Validation error
- `401 Unauthorized`: Authentication required
- `403 Forbidden`: Insufficient permissions
- `404 Not Found`: Resource not found
- `409 Conflict`: Business rule violation
- `422 Unprocessable Entity`: Semantic validation error
- `500 Internal Server Error`: Unexpected server error
- `501 Not Implemented`: Endpoint not yet implemented

---

## Correlation ID & Request Tracking

### X-Correlation-Id Header

All API requests **SHOULD** include an `X-Correlation-Id` header for distributed tracing:

```http
GET /v1/order/entities/123
X-Correlation-Id: abc-123-def-456
```

### Response Headers

All API responses **MUST** echo the correlation ID:

```http
HTTP/1.1 200 OK
X-Correlation-Id: abc-123-def-456
```

### Error Responses

All error responses **MUST** include the correlation ID in the body:

```json
{
  "code": "NOT_FOUND",
  "message": "Entity not found",
  "correlationId": "abc-123-def-456"
}
```

**Reference:** See `docs/architecture/api/BACKEND_CONTRACT_GLOBAL_STANDARDS.md` for correlation ID conventions.

---

## API Endpoints

### Endpoint Summary

This domain exposes **14** REST API endpoints:

| Method | Path | Summary |
|--------|------|---------|
| POST | `/v1/orders/carts` | Create a sales order cart |
| GET | `/v1/orders/carts/{orderId}` | Get a sales order by ID |
| POST | `/v1/orders/carts/{orderId}/cancel` | Cancel order |
| POST | `/v1/orders/carts/{orderId}/cancel/retry` | Retry failed cancellation |
| POST | `/v1/orders/carts/{orderId}/items` | Add an item to a sales order cart |
| DELETE | `/v1/orders/carts/{orderId}/items/{lineId}` | Remove an item from a sales order cart |
| PUT | `/v1/orders/carts/{orderId}/items/{lineId}` | Update a sales order cart item quantity |
| PATCH | `/v1/orders/carts/{orderId}/source` | Link a source to a sales order |
| GET | `/v1/orders/price-overrides` | Get price overrides |
| POST | `/v1/orders/price-overrides` | Apply price override |
| GET | `/v1/orders/price-overrides/pending` | Get pending approvals |
| GET | `/v1/orders/price-overrides/{overrideId}` | Get price override |
| POST | `/v1/orders/price-overrides/{overrideId}/approve` | Approve price override |
| POST | `/v1/orders/price-overrides/{overrideId}/reject` | Reject price override |

### Endpoint Details

#### POST /v1/orders/carts

**Summary:** Create a sales order cart

**Description:** Create a new sales order cart for a customer, terminal, and optional vehicle context.

**Operation ID:** `createCart`

**Responses:**

- `200`: OK


---

#### GET /v1/orders/carts/{orderId}

**Summary:** Get a sales order by ID

**Description:** Retrieve a sales order cart and its current line items by identifier.

**Operation ID:** `getOrder`

**Parameters:**

- `orderId` (path, Required, string): 

**Responses:**

- `200`: OK


---

#### POST /v1/orders/carts/{orderId}/cancel

**Summary:** Cancel order

**Description:** Initiate cancellation for a sales order cart using the supplied cancellation context.

**Operation ID:** `cancelOrder`

**Parameters:**

- `orderId` (path, Required, string): 

**Responses:**

- `201`: Cancellation initiated
- `400`: Invalid request
- `403`: Insufficient permissions
- `404`: Order not found
- `409`: Order cannot be cancelled in current state


---

#### POST /v1/orders/carts/{orderId}/cancel/retry

**Summary:** Retry failed cancellation

**Description:** Retry a previously failed order cancellation using the original idempotency key.

**Operation ID:** `retryCancellation`

**Parameters:**

- `orderId` (path, Required, string): 
- `idempotencyKey` (query, Required, string): 

**Responses:**

- `200`: Retry accepted
- `403`: Insufficient permissions
- `404`: Order not found
- `409`: Order not in retryable state


---

#### POST /v1/orders/carts/{orderId}/items

**Summary:** Add an item to a sales order cart

**Description:** Add a line item to an existing sales order cart using SKU, quantity, and optional pricing context.

**Operation ID:** `addItem`

**Parameters:**

- `orderId` (path, Required, string): 

**Responses:**

- `200`: OK


---

#### DELETE /v1/orders/carts/{orderId}/items/{lineId}

**Summary:** Remove an item from a sales order cart

**Description:** Remove a line item from an existing sales order cart.

**Operation ID:** `removeItem`

**Parameters:**

- `orderId` (path, Required, string): 
- `lineId` (path, Required, string): 

**Responses:**

- `200`: OK


---

#### PUT /v1/orders/carts/{orderId}/items/{lineId}

**Summary:** Update a sales order cart item quantity

**Description:** Update the quantity for an existing sales order line item in a cart.

**Operation ID:** `updateItemQuantity`

**Parameters:**

- `orderId` (path, Required, string): 
- `lineId` (path, Required, string): 

**Responses:**

- `200`: OK


---

#### PATCH /v1/orders/carts/{orderId}/source

**Summary:** Link a source to a sales order

**Description:** Associate an external source reference with an existing sales order cart.

**Operation ID:** `linkSource`

**Parameters:**

- `orderId` (path, Required, string): 

**Responses:**

- `200`: OK


---

#### GET /v1/orders/price-overrides

**Summary:** Get price overrides

**Description:** Retrieve price overrides by order ID, status, or date range. At least one filter parameter is required.

**Operation ID:** `getOverridesByOrder`

**Parameters:**

- `orderId` (query, Optional, string): Order ID filter
- `status` (query, Optional, string): Override status filter
- `startDate` (query, Optional, string): Start date for date range filter
- `endDate` (query, Optional, string): End date for date range filter

**Responses:**

- `200`: Overrides retrieved
- `400`: No filter parameter provided


---

#### POST /v1/orders/price-overrides

**Summary:** Apply price override

**Description:** Apply a price override to an order line. May require approval based on override amount.

**Operation ID:** `applyPriceOverride`

**Responses:**

- `201`: Override created
- `400`: Invalid request
- `403`: Insufficient permissions


---

#### GET /v1/orders/price-overrides/pending

**Summary:** Get pending approvals

**Description:** Retrieve all price overrides awaiting approval.

**Operation ID:** `getPendingApprovals`

**Responses:**

- `200`: Pending overrides retrieved


---

#### GET /v1/orders/price-overrides/{overrideId}

**Summary:** Get price override

**Description:** Retrieve a specific price override by ID.

**Operation ID:** `getOverride`

**Parameters:**

- `overrideId` (path, Required, string): Price override ID

**Responses:**

- `200`: Override found
- `404`: Override not found


---

#### POST /v1/orders/price-overrides/{overrideId}/approve

**Summary:** Approve price override

**Description:** Approve a pending price override. Validates approver permission level.

**Operation ID:** `approvePriceOverride`

**Parameters:**

- `overrideId` (path, Required, string): Price override ID

**Responses:**

- `200`: Override approved
- `400`: Invalid request or override not in pending state
- `403`: Insufficient approval permissions
- `404`: Override not found


---

#### POST /v1/orders/price-overrides/{overrideId}/reject

**Summary:** Reject price override

**Description:** Reject a pending price override with a reason.

**Operation ID:** `rejectPriceOverride`

**Parameters:**

- `overrideId` (path, Required, string): Price override ID

**Responses:**

- `200`: Override rejected
- `400`: Invalid request or override not in pending state
- `403`: Insufficient rejection permissions
- `404`: Override not found



---

## Entity-Specific Contracts

### AddItemRequest

Request payload for adding an item line to a cart

**Fields:**

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `itemSku` | string | Yes | Stock keeping unit identifying the item to add |
| `manualPrice` | number | No | Manual unit price override for the item, when applicable |
| `quantity` | integer (int32) | Yes | Quantity of the item to add to the cart |
| `reasonCode` | string | No | Reason code justifying a manual price or special handling |


### ApplyPriceOverrideRequest

Request payload for applying a price override to an order line

**Fields:**

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `idempotencyKey` | string | No | Optional idempotency key for duplicate prevention |
| `justification` | string | No | Free-text justification supporting the price override |
| `orderId` | string | Yes | Identifier of the order whose line is being overridden |
| `orderLineId` | string | Yes | Identifier of the order line receiving the price override |
| `originalPrice` | number | Yes | Original price of the line before the override |
| `overridePrice` | number | Yes | Overridden price to apply to the line |
| `productId` | string | Yes | Identifier of the product on the order line |
| `reasonCode` | string | Yes | Reason code for the price override |


### ApprovePriceOverrideRequest

Request payload for approving a pending price override

**Fields:**

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `comments` | string | No | Optional approver comments recorded with the approval |


### CancelOrderRequest

Request payload for cancelling an order

**Fields:**

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `cancellationReason` | string | Yes | Reason explaining why the order is being cancelled |
| `idempotencyKey` | string | No | Optional idempotency key for duplicate cancellation prevention |
| `paymentId` | string (uuid) | No | Identifier of the associated payment, when applicable |
| `workOrderId` | string (uuid) | No | Identifier of the associated workorder, when applicable |


### CancellationResponse

Response returned after cancelling an order

**Fields:**

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `cancellationIdempotencyKey` | string | No | Idempotency key associated with the cancellation request |
| `message` | string | No | Human-readable message describing the cancellation outcome |
| `orderId` | string | Yes | Identifier of the cancelled order |
| `status` | string | Yes | Resulting status of the order after cancellation |


### CreateCartRequest

Request payload for creating a new cart

**Fields:**

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `clerkId` | string | Yes | Identifier of the clerk creating the cart |
| `customerId` | string | No | Identifier of the customer associated with the cart, when known |
| `terminalId` | string | Yes | Identifier of the terminal where the cart is created |
| `vehicleId` | string | No | Identifier of the vehicle associated with the cart, when applicable |


### LinkSourceRequest

Request payload for linking an order to an originating source

**Fields:**

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `sourceId` | string | Yes | Identifier of the originating source record |
| `sourceType` | string | Yes | Type classification of the originating source |


### PriceOverrideDetail

Detailed view of a price override including approval workflow state

**Fields:**

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `affectsCommission` | boolean | Yes | Whether the override affects sales commission |
| `appliedAt` | string (date-time) | No | Timestamp when the override was applied to the line, if applied |
| `approvedAt` | string (date-time) | No | Timestamp when the override was approved, if approved |
| `approvedByUserId` | string | No | Identifier of the user who approved the override, if approved |
| `createdAt` | string (date-time) | Yes | Timestamp when the override was created |
| `discountAmount` | number | Yes | Absolute discount amount granted by the override |
| `discountPercentage` | number | Yes | Discount expressed as a percentage of the original price |
| `justification` | string | No | Free-text justification supporting the override |
| `orderId` | string | Yes | Identifier of the order the override applies to |
| `orderLineId` | string | Yes | Identifier of the order line the override applies to |
| `originalPrice` | number | Yes | Original price before the override |
| `overrideId` | string (uuid) | Yes | Unique identifier of the price override |
| `overridePrice` | number | Yes | Overridden price applied to the line |
| `productId` | string | Yes | Identifier of the product on the overridden line |
| `reasonCode` | string | Yes | Reason code supplied for the override |
| `rejectedAt` | string (date-time) | No | Timestamp when the override was rejected, if rejected |
| `rejectedByUserId` | string | No | Identifier of the user who rejected the override, if rejected |
| `rejectionReason` | string | No | Reason the override was rejected, if rejected |
| `requestedByUserId` | string | Yes | Identifier of the user who requested the override |
| `requiresApproval` | boolean | Yes | Whether the override required approval |
| `status` | string | Yes | Current override status (e.g. APPROVED, PENDING_APPROVAL, REJECTED) |
| `updatedAt` | string (date-time) | No | Timestamp when the override was last updated |


### PriceOverrideResult

Result of applying a price override to an order line

**Fields:**

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `affectsCommission` | boolean | Yes | Whether the override affects sales commission |
| `createdAt` | string (date-time) | Yes | Timestamp when the override was created |
| `discountAmount` | number | Yes | Absolute discount amount granted by the override |
| `discountPercentage` | number | Yes | Discount expressed as a percentage of the original price |
| `justification` | string | No | Free-text justification supporting the override |
| `message` | string | No | Optional human-readable message describing the result |
| `orderId` | string | Yes | Identifier of the order the override applies to |
| `orderLineId` | string | Yes | Identifier of the order line the override applies to |
| `originalPrice` | number | Yes | Original price before the override |
| `overrideId` | string (uuid) | Yes | Unique identifier of the created price override |
| `overridePrice` | number | Yes | Overridden price applied to the line |
| `productId` | string | Yes | Identifier of the product on the overridden line |
| `reasonCode` | string | Yes | Reason code supplied for the override |
| `requestedByUserId` | string | Yes | Identifier of the user who requested the override |
| `requiresApproval` | boolean | Yes | Whether the override required approval |
| `status` | string | Yes | Override status; APPROVED means applied immediately, PENDING_APPROVAL means queued for review |


### RejectPriceOverrideRequest

Request payload for rejecting a pending price override

**Fields:**

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `comments` | string | No | Optional reviewer comments recorded with the rejection |
| `reason` | string | Yes | Reason explaining why the override is being rejected |


*Additional schemas omitted for brevity. See OpenAPI spec for complete list.*

---

## Examples

### Example Request/Response Pairs

#### Example: Create Request

```http
POST /v1/orders/price-overrides
Content-Type: application/json
X-Correlation-Id: abc-123-def-456

{
  "name": "Example",
  "description": "Example description",
  "status": "ACTIVE"
}
```

**Response:**

```http
HTTP/1.1 201 Created
X-Correlation-Id: abc-123-def-456

{
  "id": "new-id-123",
  "name": "Example",
  "description": "Example description",
  "status": "ACTIVE",
  "createdAt": "2026-01-27T14:30:00Z"
}
```

#### Example: Retrieve Request

```http
GET /v1/orders/price-overrides
X-Correlation-Id: abc-123-def-456
```

**Response:**

```http
HTTP/1.1 200 OK
X-Correlation-Id: abc-123-def-456

{
  "id": "existing-id-456",
  "name": "Example",
  "status": "ACTIVE",
  "createdAt": "2026-01-27T14:00:00Z",
  "updatedAt": "2026-01-27T14:30:00Z"
}
```

---

## Summary

This generated reference summarizes API structures for the Order Management domain:

- **Field Naming**: camelCase for all JSON fields
- **Enum Values**: UPPER_SNAKE_CASE for all enums
- **Timestamps**: ISO 8601 UTC format
- **Identifiers**: String-based UUIDs
- **Pagination**: Zero-based with standard response format
- **Error Handling**: Consistent error response structure with correlation IDs

---

## Change Log

| Version | Date | Changes |
|---------|------|---------|
| 1.0 | 2026-07-16 | Generated API reference from OpenAPI spec |

---

## References

- OpenAPI Specification: `pos-order/openapi.yaml`
- Curated Domain Contract Guide: `domains/order/.business-rules/BACKEND_CONTRACT_GUIDE.md`
- Domain Agent Guide: `domains/order/.business-rules/AGENT_GUIDE.md`
- Global Contract Standards (incl. correlation ID conventions): `docs/architecture/api/BACKEND_CONTRACT_GLOBAL_STANDARDS.md`

---

**Generated:** 2026-07-16 17:37:18 UTC  
**Tool:** `scripts/generate_backend_contract_guides.py`
