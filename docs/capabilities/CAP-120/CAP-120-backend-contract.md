# CAP:120 — Backend Contract Summary

## Capability
- id: CAP:120
- name: '[CAP] Timekeeping (Clock, Breaks, Approvals, Export)'
- domain: people

## Source of truth
- OpenAPI (authoritative): `durion-positivity-backend/pos-people/openapi.json` (server: `http://localhost:8085`)
- Capability manifest: `docs/capabilities/CAP-120/CAPABILITY_MANIFEST.yaml`

## Summary of changes applied to domain guide
- Aligned `domains/people/.business-rules/BACKEND_CONTRACT_GUIDE.md` with the current OpenAPI.
- Converted example endpoint headings to API Gateway invocation format: `http://localhost:8080/v{version}/people/...`.
- Normalized example payloads to match OpenAPI schemas (notably `UserRoleDto` uses date-only `yyyy-MM-dd` for `startDate`/`endDate`).
- Added explicit implementation/backlog link to backend child issue: https://github.com/louisburroughs/durion-positivity-backend/issues/79

## Authoritative endpoints (extracted from OpenAPI)
- GET http://localhost:8080/v1/people/{personId}
- PUT http://localhost:8080/v1/people/{personId}
- DELETE http://localhost:8080/v1/people/{personId}
- GET http://localhost:8080/v1/people
- POST http://localhost:8080/v1/people
- GET http://localhost:8080/v1/people/{personUuid}/access/roles
- GET http://localhost:8080/v1/people/{personUuid}/access/assignments
- POST http://localhost:8080/v1/people/{personUuid}/access/assignments
- DELETE http://localhost:8080/v1/people/{personUuid}/access/assignments/{roleCode}
- POST http://localhost:8080/v1/people/workSessions/start
- POST http://localhost:8080/v1/people/workSessions/stop
- POST http://localhost:8080/v1/people/workSessions/{id}/breaks/start
- POST http://localhost:8080/v1/people/workSessions/{id}/breaks/stop
- POST http://localhost:8080/v1/people/timeEntries/approve
- POST http://localhost:8080/v1/people/timeEntries/reject
- POST http://localhost:8080/v1/people/timeEntries/adjustments
- ... (full list lives in the OpenAPI file)

## Provider test hints
- ContractBehaviorIT should call through the API Gateway URL `http://localhost:8080/v1/people/...`.
- When asserting `UserRoleDto` responses, assert `startDate`/`endDate` are date-only strings (`YYYY-MM-DD`).
- For assignment create (`POST /v1/people/{personUuid}/access/assignments`) the request uses date-time values for `startDate`/`endDate`; provider maps those to `UserRoleDto` date-only fields on response.
- Use `X-User-Id`, `X-Permissions`, and `X-Correlation-Id` headers in integration/provider tests.

## Implementation links
- Backend child issue (manifest): https://github.com/louisburroughs/durion-positivity-backend/issues/79
- Legacy coordination: https://github.com/louisburroughs/durion-positivity-backend/issues/86

## Notes and TODOs
- TODO: Confirm whether provider should accept and return time components for role assignment dates; currently OpenAPI models `UserRoleDto` start/end as `date` (no time). See backend issue #79 for discussion.
- TODO: Ensure API Gateway rewrites keep `X-API-Version` or equivalent header as required by gateway routing.

---

Generated by automated contract guide updater.
